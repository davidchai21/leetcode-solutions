This question is easy. We have many ways to deal with it. One time consuming way is to use 2 for loops to traverse and find the indices. This can take O(n2) time in worst case. 

Another way is to build an unordered_map and traverse again. This can be O(n) time, which is much faster.

Runtime: 6ms
