This question is very easy. We have at least 2 ways to solve it. The direct way is to build a map and compare the difference, then output the character.

Another good way to do so is to use XOR as bit manipulation. Because there is only one char value appears once, so the result will be the one.

Runtime: 3ms
